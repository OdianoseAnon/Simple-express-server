# Simple-express-server
Submission for an assignment. Download both route.js and index.js. Use npm i to install the package. use node index.js to run the program. output should be a list of names in a json formatt
